# Active work

このdirectoryには未完了のwork itemだけを置く。各fileはcommit単位のchecklistを持ち、
最終itemを完了したcommitで削除する。

完了後も必要な設計判断は`.dev/designdoc/`または`.dev/adr/`へ、会話固有の引き継ぎは
`.dev/contexts/`へ残す。完了済みTODOのarchiveはGit historyを使う。
