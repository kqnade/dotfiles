metapkgs/
scripts/
.gitignore
README.md
CLAUDE.md
dot_config/nvim/README.md

{{- /* Brewfile is for macOS Homebrew or non-sudo Linux Linuxbrew. */ -}}
{{ if eq .chezmoi.os "windows" -}}
Brewfile
{{- end }}

{{- /* Aptfile is only meaningful on Debian-family distributions. */ -}}
{{ if ne .chezmoi.os "linux" -}}
Aptfile
{{- end }}
{{- $id := "" -}}{{- $idLike := "" -}}
{{- if eq .chezmoi.os "linux" -}}
  {{- $id = (.chezmoi.osRelease.id | default "") -}}
  {{- $idLike = (index .chezmoi.osRelease "idLike" | default "") -}}
{{- end }}
{{ if and (eq .chezmoi.os "linux") (not (or (eq $id "ubuntu") (eq $id "debian") (contains "debian" $idLike))) -}}
Aptfile
{{ end }}
{{- /* Files only meaningful on Windows */ -}}
{{ if ne .chezmoi.os "windows" -}}
Documents/
scoopfile.json
{{- end }}

{{- /* Files only meaningful on Linux / macOS */ -}}
{{ if eq .chezmoi.os "windows" -}}
dot_zshrc
dot_config/zsh/
dot_config/sheldon/
dot_config/ghostty/
dot_config/kitty/
private_dot_gnupg/
{{- end }}
