{
  plugins.neo-tree = {
    enable = true;
    closeIfLastWindow = true;
    extraSources = [
      "buffers"
      "git_status"
      "filesystem"
    ];
    window = {
      width = 35;
    };
  };
}
